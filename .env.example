# AI Multimodal Intelligence
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-flash-latest"

# Platform Configuration
PORT=3000
NODE_ENV="development"

# App URL for public routing and tenant resolution
APP_URL="http://localhost:3000"

# Required in production: generate a unique random secret (at least 32 bytes).
SESSION_SECRET=""

# Private key for the unlinked platform-owner archive deletion screen.
PLATFORM_ADMIN_KEY=""

# Durable archive storage (required for public production deployment)
SUPABASE_URL=""
SUPABASE_SECRET_KEY=""
